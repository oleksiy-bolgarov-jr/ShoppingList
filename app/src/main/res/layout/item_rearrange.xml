<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/ll_rearrangeable_item"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    tools:ignore="UseCompoundDrawables">

    <ImageView
        android:id="@+id/iv_rearrange_drag_handle"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:contentDescription="@string/rearrange_content_description_drag_handle"
        android:gravity="center_vertical"
        android:src="@drawable/ic_drag_handle_black_24dp" />

    <TextView
        android:id="@+id/tv_rearrange_item_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:padding="16dp"
        android:textColor="@color/black"
        android:textSize="@dimen/text_size_variable" />

</LinearLayout>